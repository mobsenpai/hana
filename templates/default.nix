{
  c-dev-shell = {
    path = ./c-dev-shell;
    description = "C dev shell flake";
  };

  minimal-dev-shell = {
    path = ./minimal-dev-shell;
    description = "Minimal dev shell flake";
  };

  python-dev-shell = {
    path = ./python-dev-shell;
    description = "Python dev shell flake";
  };

  web-dev-shell = {
    path = ./web-dev-shell;
    description = "WebDev shell flake";
  };
}
